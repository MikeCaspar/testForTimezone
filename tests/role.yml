---
- hosts: localhost
  remote_user: root

  roles:

    - { role: testForTimezone, timezoneCity: "America/Toronto", debug: true }
    - { role: testForTimezone, timezoneCity: "America/Montreal", debug: true }

